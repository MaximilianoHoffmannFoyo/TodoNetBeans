package act3;

/*C) Programador, especialización de Empleado. 

Atributos: categoría (que puede ser “Analista”, “Senior” y “Junior”). Implementar usando Enum.
Métodos: 
- Getter y Setter.
- Constructores (por defecto y parametrizado)
- Método toString(), que sobrescribe el método toString() de la superclase, 
añadiendo además de los atributos de empleado los propios de programador.
Dibuja el diagrama de clases UML correspondiente a la jerarquía.
Realiza una clase Prueba, para probar las clases anteriores. Usa los constructores para crear un par de objetos de cada clase y comprueba como un objeto de una clase hija puede llamar a métodos de la clase padre, pero no al contrario. Ejecuta los métodos toString() de cada uno de los objetos y comprueba el resultado.

 */
public class Programador {
    
    
    
}
